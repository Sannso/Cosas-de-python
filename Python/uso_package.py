from package_calculos.calculos_basicos.operaciones_basicas import * # sumar, restar, multiplicar y dividir

result = sumar(2,3)
print("\nEl resultado de la operacion: " + str(result))

from package_calculos.calculos_semi.operaciones_semi import potencia  # Es la unica que hay por ahora por eso la pongo

result2 = potencia(2,4)
print("\nEl resultado de la potencia: " + str(result2))
